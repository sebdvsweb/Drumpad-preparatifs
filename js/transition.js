// on récupère toutes les div de class 'key'

// on crée un tableau associatif JS avec les keyCode qui nous intéresse


// on ajoute un écouteur d'évènements sur notre clavier de PC
  

        // on affiche le keycode dans la balise <p> vide

        // on affiche la lettre sur le bouton bleu
  
        // pour chaque div qui a la class key

            // si le keyCode de la touche cliquée correspond au code de l'attribut HTML data-key dans les <div class='key'>
            
            // alors on ajoute la class CSS bleu
            
            // une fois la transition CSS finie
            
            // on retire la class CSS bleu
